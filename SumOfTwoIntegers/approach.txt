1) We are required to add two numbers without using + or - anywhere in the program
2) We utilize a special property that uses XOR and AND operations to maintain sum and carry
3) By performing XOR and SUM with left shift (for carry), we get the sum